filters:
  and:
    - status == "revising"
    - file.inFolder("03-Projects")
formulas:
  stale: (now() - file.mtime).days
views:
  - type: table
    name: 待我接管
    order:
      - file.name
      - topic
      - version
      - updated
      - due
      - formula.stale
