---
type: usage-guide
updated: 2026-09-09
---
# 使用指南

> 本库的两个用途：**① 与 AI 共同创作**（AI 在 `03-Projects/` 内可写）；**② 作为 AI 工具的知识输入**（AI 全库只读检索）。
> 权限、状态机、目录语义都已在根目录 `CLAUDE.md` 里对 AI 声明；本文是给人看的操作手册。

---

## 目录

- [1. 首次启动（一次性，约 10 分钟）](#1-首次启动一次性约-10-分钟)
- [2. 目录地图与内容模型](#2-目录地图与内容模型)
- [3. 必须理解的四条核心规则](#3-必须理解的四条核心规则)
- [4. 日常工作流（按场景）](#4-日常工作流按场景)
- [5. 接入 AI 工具与命令地图](#5-接入-ai-工具与命令地图)
- [6. 看板视图](#6-看板视图)
- [7. 每周维护清单](#7-每周维护清单)
- [8. 常见问题与避坑](#8-常见问题与避坑)
- [9. 如何自定义](#9-如何自定义)

---

## 1. 首次启动（一次性，约 10 分钟）

### 1.1 用 Obsidian 打开本库
> 初始化：先在库根跑 `bash init-vault.sh`（自动填 MCP 路径；可选 `--areas "领域A,领域B"` 自定义领域）。

1. Obsidian → 「打开文件夹作为仓库（Open folder as vault）」→ 选择 `G:\Documents\repository\obsidian-next`。
2. 弹出「信任作者并启用插件」时选择**信任**（否则社区插件无法运行）。

### 1.2 启用核心模板插件

- 设置 → 核心插件 → 打开「**模板（Templates）**」。
- 模板目录已预配置为 `00-System/templates`（`.obsidian/templates.json`），无需再设；可到 设置 → 模板 里核对。
- 模板现在的用途：`tpl-source`（手动收素材）、`tpl-note-card`（笔记卡）、`tpl-draft`（手写草稿）——项目骨架（`_brief/challenges/journal`）已由脚本自动生成，不需要手动套模板。

### 1.3 安装社区插件（按需）

设置 → 第三方插件 → 关闭安全模式 → 浏览：

| 插件                                    | 必要性  | 作用                                                                                               |
| ------------------------------------- | ---- | ------------------------------------------------------------------------------------------------ |
| **Vault Operator**                    | 可选   | 治理**经 Obsidian 的 AI 写入**（执行 `.obsidian-agentignore` / `.obsidian-agentprotected`）+ 影子快照；**管不到外部 AI 直写文件**（实测影子仓库不记录外部变更）——外部工具为主时可装可不装 |
| **Kanban Bases View**（或同类 Bases 看板插件） | 可选   | 共创看板可拖拽卡片直接改 `status`                                                                            |
| **Claudian**                          | 可选   | Obsidian 内嵌 AI，词级 diff 编辑体验最好；**主方式是外部 AI 工具（§5.2）**，只在不愿切窗口时启用（取舍见 §5.3）                        |
| **Obsidian Git**                      | 可选   | 图形化 commit；**务必关自动 commit**，改为手动                                                                 |

> 插件名以社区市场实际搜索结果为准。**外部 AI 工具为主方式时，没有必须装的社区插件**——硬边界在 MCP（§5.2），快照/后悔药由 git 手动 commit 承担（§1.4）。Vault Operator 只在"要在 Obsidian 内使用 AI 插件"时才变得有价值。

### 1.4 Git 习惯（库已初始化，含基线 commit）

- **手动 commit，节奏 = 一轮共创结束时**：

```bash
cd /path/to/your/vault
git add -A && git commit -m "共创：第3节 v2 完成"
```

- **不要开自动 commit**（AI 逐段写作会把它切碎成几十个无意义提交）。
- git 提交同时是本库的**快照/后悔药**：Vault Operator 的影子快照只覆盖经 Obsidian 的写入，外部 AI 的直写只有 git 兜底——重要节点宁可多 commit。
- 治理文件（`CLAUDE.md`、`.claude/settings.json`、`.mcp.json`、`.obsidian-agent*`）已入库；`git log` 能审计它们是否被改过。

---

## 2. 目录地图与内容模型

### 2.1 顶层结构与权限

```
obsidian-next/
├── 00-System/    治理层：模板、看板、脚本     AI 只读
├── 01-Inbox/     唯一入口，每周清空          AI 可写
├── 02-Areas/     长期领域（你的判断）     AI 只读
├── 03-Projects/  在途创作项目（共创区）       AI 受限可写（见 §2.2）
├── 04-Resources/ clippings/ 原文层 + 风格库   AI 只读
├── 05-Archive/   归档                        AI 只读
├── _INDEX.md     总入口
├── CLAUDE.md     AI 宪法（AI 每次会话先读）
└── AGENTS.md     指向 CLAUDE.md
```

### 2.2 项目内四层分区（`03-Projects/<主题>/`）

| 目录 | 放什么 | AI 权限 |
|---|---|---|
| `10-source/` | 剪藏 clippings、访谈 interviews、灵感 sparks | **只读**（append-only 由你维护） |
| `20-notes/` | 摘要卡、论点卡、案例卡 | 可读写 |
| `30-drafts/` | v1/ v2/ 迭代稿 + `_baseline/` 基线 | 可读写 |
| `40-review/` | 你正在人工修改的版本 | 只读 |
| `50-final/` | 定稿 | **禁写** |
| `60-published/` | 已分发副本 | 禁写 |

数字前缀 = 流程方向：10 原料 → 20 加工 → 30 共创 → 40 接管 → 50 定稿 → 60 分发。

### 2.3 三层内容模型（决定"一篇东西放哪"）

| 层 | 位置 | 放什么 | 谁写 |
|---|---|---|---|
| **原文层** | `04-Resources/clippings/` | 第三方文章，原样收藏不改写 | 你（`/intake/clip` 备料） |
| **结论层** | `02-Areas/<领域>/` | 你的判断（"我"开头的结论，两年后仍成立） | 只有人（`/knowledge/note` 备料） |
| **原料层** | `03-Projects/<项目>/10-source/` | 服务当前创作的素材副本 | 只有你 |

一句话记忆：**Projects 产出作品（给别人看的），Clippings 收别人的原文，Areas 存我的判断。**
典型流：收藏文章进 clippings → 读完写判断进 Areas（链回原文）→ 下个项目开题时 AI 读你的判断当写作背景 → 项目完结结论还债回 Areas。

---

## 3. 必须理解的四条核心规则

### 3.1 状态机（你和 AI 对"现在到哪了"的共识）

```
idea → outline → drafting → revising → review → final → published → archived
灵感    大纲      AI出初稿    人工改结构   事实核查    定稿     已分发      归档
```

状态写在每个产出文件的 frontmatter `status` 字段里。看板按它分组；你拖卡片 = 改状态。

### 3.2 版本规则：新版本新文件，永不覆盖

- AI 出 v1 → 你拷贝到 `40-review/` 改 → 需要 AI 再迭代时，让它写 `30-drafts/v2/`，**v1 原样保留**。
- AI 每次出稿**必须同时**在 `30-drafts/_baseline/` 存一份同名的 untouched 副本——这是事后 diff、量化人机贡献的唯一依据。

### 3.3 AI 参与度标注（每份产出都要写）

```yaml
authored_by: ai-assisted   # human | ai-assisted | ai-generated
ai_level: copilot          # none | hint | assist | pair | copilot | auto
ai_model: glm-4.7
```

用途：事后用「AI 参与度审计」视图筛出 AI 高参与的内容，重点做事实核查。

### 3.4 human_locked（字段级锁）

你亲自接管的笔记在 frontmatter 加 `human_locked: true`，AI 只能**提议**修改、不能直接写入。定稿入 `50-final/` 时必加。

---

## 4. 日常工作流（按场景）

### 4.1 随手收集（随时）

任何想法/链接先扔进 `01-Inbox/`，**不分类**。对 AI 说一句 `/intake/collect <内容>` 即可，或自己拖文件进去。分类是每周清理时的事（降低收集摩擦）。

### 4.2 收藏第三方文章（随时）

1. 遇到好文章 → `/intake/clip <URL 或本地文件>`：AI 抓全文**原样**暂存进 Inbox（不改写、不删节、不总结），你拖进 `04-Resources/clippings/`（双链自动跟随）。
2. 读完有想法 → `/knowledge/note <领域> <我的判断>`：判断（不是文章摘抄）暂存 Inbox + 生成 `_context.md` 待粘贴增量，你审定后落进 Areas。
3. 分界线：**文章本体永远只在 clippings；你的同意/反对去 Areas 并链回原文**。
4. 隐私：访谈对象、未公开数据、他人稿件不要经云端模型处理。

### 4.3 每周清理（5–10 分钟）

让 AI 跑 `/intake/triage`：逐条分析 Inbox 并给出去向计划，你确认后执行。三去向：
1. 与当前创作相关 → 整理进 `03-Projects/<主题>/10-source/`（AI 可先在 Inbox 内补好 frontmatter，移动由你执行）；
2. 属于长期领域 → 原文类移入 `04-Resources/clippings/`；判断类用 `/knowledge/note` 提炼后移入 `02-Areas/<领域>/`；
3. 没价值 → 删（删除永远人工执行，AI 的 rm 是 deny 的）。

### 4.4 开一个新创作项目（checklist）

**第 1 步：建目录**——三种方式任选：
- 让 AI 跑 `/produce/new 你的主题名`（脚本脚手架 + AI 填简报草稿 + 核对后的待粘贴索引行）；
- 直接跑脚本（无需 AI 会话）：`bash 00-System/scripts/new-project.sh 你的主题名 [mini]`；
- 手动复制示例项目后改名。

**第 2 步：写 `_brief.md`**（最高杠杆的一步）——新建笔记，插入 `tpl-project-brief` 模板，填全 11 节。跨领域的主题用 `area: [副业实践, 学习教育]` 软关联，**不要**把项目复制进各领域文件夹。

**第 3 步：建 `challenges.md` 和 `journal.md`**——把反对你这个项目立场的最强论据写进 challenges（防止 AI 只顺着你写）；journal 记"做了什么/学到什么/下一步"。

**第 4 步：挂索引**——在 [[_INDEX]] 的"进行中的创作主题"表里加一行；再到相关领域的 `_context.md` 的"相关项目"下加链接。（`/produce/new` 或脚本会生成这两段待粘贴内容，直接粘贴即可。）

**第 5 步（可选）**：右键 `_brief.md` → 书签，把当前主题钉在侧边栏。

### 4.5 攒项目素材（append-only，只有你动手）

- 文件名：`日期-来源-标题.md`（如 `2026-09-06-xxx-官方博客.md`）。
- 手动新建素材笔记时，用 Templates 插件插入 `tpl-source` 模板（命令面板 → Insert template）。
- frontmatter 保留 `source:`（原始 URL）和 `captured:`（时间）。
- 摘录用两栏：原文放 `>` 引用块**永不改动**，你的批注写在下方。
- **不要让 AI"顺手润色"素材**——润色版应另存到 `20-notes/`。
- 涉及访谈对象、未公开数据的内容不要贴给云端模型。

### 4.6 让 AI 加工笔记卡

`/produce/digest <项目名>`：读 `10-source/` 产出摘要卡/论点卡/案例卡到 `20-notes/`（用 `tpl-note-card` 的字段），**每张卡末尾回链素材出处**。

### 4.7 与 AI 共创初稿

`/produce/draft <项目名> <稿子标识>`：AI 读 `_brief.md` + `challenges.md` → 基于 `20-notes/` 写 `30-drafts/v1/` → 同步 `_baseline/` 存底。你全程不用重复解释背景——背景都在简报里。

### 4.8 人工修改（AI 退场）

把稿子拷到 `40-review/`，frontmatter 改 `status: revising`，随手加 `human_locked: true`。这一区 AI 只读。改完大改后，让 AI 分析你的修改模式（§4.11）。

### 4.9 定稿与分发

1. 终稿移入 `50-final/`：`status: final`、`human_locked: true`。
2. 分发到公众号/平台后，把副本放 `60-published/`，`status: published`。
3. `journal.md` 记一条卡点与教训。

### 4.10 项目归档

`/produce/archive <项目名>`：前置检查 → 脚本完成 `status` 翻转与目录移动（进 `05-Archive/<年>/`）→ AI 从 journal 提炼 2–3 条长期结论生成"待粘贴"的 Areas 回写内容。无 AI 会话时可直接跑 `bash 00-System/scripts/archive-project.sh <项目名>`，结论提炼建议回到会话补做。**结论回写是归档的核心价值（飞轮），别跳过**（机制见 §4.12"还"）。

### 4.11 风格库迭代（越用越准）

有人工大改后跑 `/quality/voice`：AI diff `_baseline/` 与 `40-review/` 两版，把你的修改模式（"他会删花哨比喻""他总是加入亲身观察"）写成分析放到 `20-notes/voice-update-<日期>.md`，**你审定后手工合并**进 [[04-Resources/voice-reference|voice-reference.md]]。这份用真实编辑证据长出的风格库，比 prompt 里写"口语化"有效得多。

### 4.12 领域认知更新机制（Areas 的写/改/还）

> 原则：**认知更新由实践触发，不由日历触发**。不存在"定期维护知识库"的仪式——每次实践产生新证据时，顺手更新受影响的判断。一条判断很久没被改，说明它还没被实践打脸，这本身是有效信息。

| 规则 | 触发时机 | 动作 |
|---|---|---|
| **写** | 想到一条判断 | 记下来：`/knowledge/note <领域> <判断>`（暂存 Inbox + 待粘贴 `_context` 增量，落库人工） |
| **改** | 用到时发现不对 | 当场改，**记修正而非覆盖**：`/knowledge/note 修正 <笔记路径>`（AI 出修正案） |
| **还** | 项目归档时 | `/produce/archive` 强制提炼 2–3 条结论回写（流程内置，不可跳过） |

修正记录格式（在判断笔记内追加，保留演化轨迹与证据量）：

```
2026-09 原判断：清单式标题打开率 ≈ 故事式 2 倍（样本 6 篇，公众号）
2027-03 修正：效果会衰减，需轮换句式（样本累计 18 篇）
```

配套规则：`_context.md` 是 500 词上限的压缩入口，加一行就想一行要不要删；AI 对 Areas 全程只读——能引用、能提修正案、能提醒停滞，落笔永远是人。

---

## 5. 接入 AI 工具与命令地图

> **主方式是外部 AI 工具**：在库根目录启动，架构所需的一切（宪法、权限、命令、MCP 硬边界）都在这条路径上。Obsidian 内 AI 插件（§5.3）只是可选的写作手感增强，**不装不影响任何功能**。

### 5.1 AI 进来后会发生什么（无论哪种方式）

```
读 CLAUDE.md（宪法+权限+路由表）
  → 按你的问题命中领域路由表 → 读 02-Areas/<领域>/_context.md
  → 涉及创作 → 读 03-Projects/<主题>/_brief.md + challenges.md
  → 才开始干活
```

### 5.2 主方式：外部 AI 工具 + MCP（Claude Code / Codex CLI 等）

架构所需的一切都在这条路径上：宪法与斜杠命令自动加载；而且**最硬的 L1 边界（写入权限钉死在 `03-Projects/`）只存在于这条路径**——任何 Obsidian 内插件都没有等价能力。

`.mcp.json` 已配好两个实例，**在库根目录启动**即自动加载：

```bash
cd /path/to/your/vault
claude
```

| 实例 | 范围 | 权限 |
|---|---|---|
| `vault-read` | 全库 | 只读检索（知识库作为 AI 输入用这个） |
| `project-rw` | 钉死在 `03-Projects/` | 可写（共创用这个） |

- 首次启动会请求批准这两个 MCP server，同意即可；首次运行 npx 会现场下载包，稍等。
- 物理边界之外还有第二层：`.claude/settings.json` 的 deny 规则会拦住它写 `10-source/`、`40-review/`、`50-final/`、`02-Areas/` 等（deny → ask → allow，deny 优先）。
- 第三层：装了 Vault Operator 后，`.obsidian-agentprotected` 对**经 Obsidian/其接口的写入**（库内 AI 插件）连显式批准也拒。注意边界：它管不到外部工具直写文件——外部路径靠上面两层。
- **实际工作形态**：终端与 Obsidian 两个窗口并排——你在 Obsidian 里读稿改稿（`40-review/`），在终端里与 AI 对话。分区设计保证 AI 写 `30-drafts/`、你写 `40-review/`，双方不碰同一文件；AI 的外部改动 Obsidian 会自动感知刷新，无冲突。

### 5.3 可选增强：Claudian 插件（Obsidian 内嵌 AI）

它提供的是**写作手感**而非能力：选中一段文字直接词级 diff 编辑、`Shift+Tab` 进 Plan Mode、`@` 引用库内文件，人和 AI 的循环在单窗口内完成。同样加载本库的 `CLAUDE.md`、`.claude/settings.json`（deny 生效）和全部斜杠命令。

两个已知短板：cwd 固定库根，**没有 MCP 的子目录钉死能力**——硬边界以外部工具的 MCP 为主；且依赖 Obsidian 常驻。

**要不要装，只看一个问题**：人工修改时愿不愿意在终端和 Obsidian 之间来回切窗口。建议先纯外部工具跑两周真实项目，切窗口觉得烦再开 Claudian——两者并存零冲突（同一套宪法、权限、命令），装着不用也没有成本。

### 5.4 其他 AI 工具当"知识库输入"（只读场景）

任何支持 MCP 的工具（Cursor、Codex 等）只需挂 `vault-read` 一个实例——把 `.mcp.json` 里 `vault-read` 那段抄进对应工具的 MCP 配置即可。

### 5.5 命令地图（按功能分组，`.claude/commands/` 同构）

| 功能组 | 命令 | 作用 | 何时用 |
|---|---|---|---|
| **收进来** `/intake/` | `/intake/collect` | 想法/链接进 Inbox | 随时 |
| | `/intake/clip` | 第三方文章暂存 → clippings | 随时 |
| | `/intake/triage` | Inbox 周清分拣 | 每周 |
| **做出来** `/produce/` | `/produce/new` | 开项目脚手架 + 简报草稿 | 开题 |
| | `/produce/digest` | 素材 → 笔记卡 | 素材攒够后 |
| | `/produce/draft` | 共创初稿（双写 `_baseline`） | 每轮创作 |
| | `/produce/iterate` | 按人工修改意图出新版 | 人工改完稿 |
| | `/produce/brief` | 简报 §9 进展快照 | 每周 |
| | `/produce/archive` | 归档 + 结论还债 | 项目完结 |
| **沉淀下** `/knowledge/` | `/knowledge/note` | 领域判断写 / 修正案 | 读完有判断 / 发现过时 |
| **把好关** `/quality/` | `/quality/factcheck` | 事实溯源三档清单 | 出稿后 |
| | `/quality/voice` | 修改模式分析 → 风格库 | 人工大改后 |
| **查得到** `/query/` | `/query/search` | 全库检索（走路由表） | 随时 |
| | `/query/status` | 状态总览 + 停滞提醒 | 每周 / 开工前 |

> **命令的两种实现**：确定性操作（建目录、移动、统计、翻状态字段）由 `00-System/scripts/` 的 bash 脚本执行——零幻觉、零 token、可脱离 AI 会话直接运行（`bash 00-System/scripts/status.sh`）；判断性操作（写作、分析、分拣）是提示词命令。混合命令 = 脚本干活 + AI 解读：`/query/status`（status.sh）、`/produce/new`（new-project.sh）、`/produce/archive`（archive-project.sh）。`.gitattributes` 已强制 `*.sh` 用 LF，保证 Windows/Linux 双端可运行。

**五条功能流（场景串联）**：

- **收藏流**（平时）：`/intake/clip` → 读完 → `/knowledge/note`
- **周维护流**（约 10 分钟）：`/query/status` → `/intake/triage` → `/produce/brief` → 手动 git commit
- **共创流**（一轮创作）：`/produce/draft` → 人工改 `40-review/` → `/produce/iterate` → `/quality/factcheck` → 定稿 `50-final/` →（有人工大改则 `/quality/voice`）
- **项目全周期**：`/produce/new` → `/produce/digest` → 共创流 ×N → `/produce/archive` → 结论回写 Areas
- **知识库流**（目标②，只读）：任意 AI 工具挂 vault-read → `/query/search`

**人机分工四模式**（"所有操作都有 AI 完成或参与"的实现方式）：

0. **脚本执行**（纯机械操作）：建目录、移动、统计、翻状态——`00-System/scripts/`，零幻觉，无需会话；
1. **直写**（AI 可写区）：Inbox、20-notes、30-drafts——AI 直接落盘；
2. **备料**（AI 只读区要落内容时）：Areas、clippings——AI 在 Inbox 暂存好成品 + 生成待粘贴增量，人只做"拖过去 + 贴一下"；
3. **建议**（破坏性/治理动作）：删除、索引变更、权限调整——AI 出执行计划，人拍板执行。

永远保留人工的三样：**硬删除**（rm 对 AI 是 deny，防不可逆）、**治理文件**（AI 改不了自己的枷锁）、**信任层落库**（Areas/clippings 的最终写入）。

### 5.6 附：无斜杠命令时的等价指令（贴给任何 AI）

**加工素材 → 笔记卡：**
> 读取 `03-Projects/示例-NoteAI课程/_brief.md`，然后处理 `10-source/` 下所有素材：每篇产出一张摘要卡写入 `20-notes/`，按 tpl-note-card 的 frontmatter 字段填写，每张卡末尾必须回链素材出处。不要修改 10-source/ 里的任何文件。

**出初稿：**
> 读取 `_brief.md` 和 `challenges.md`，基于 `20-notes/` 的笔记卡写《第 3 节》初稿到 `30-drafts/v1/`，同时在 `30-drafts/_baseline/` 存一份同名 untouched 副本。frontmatter：status: drafting、authored_by: ai-generated、ai_level: copilot。行文正面回应 challenges.md 里的反方论据。

**迭代新版本：**
> 对比 `40-review/第3节.md` 与 `30-drafts/_baseline/第3节.md`，理解我的修改意图后，写 v2 到 `30-drafts/v2/`，不要动 v1。

**分析我的修改模式：**
> diff `30-drafts/_baseline/第3节.md` 和 `40-review/第3节.md`，归纳我的编辑习惯（删什么、加什么、句式偏好），写成 `20-notes/voice-update-2026-09-08.md` 供我合并进 voice-reference。不要直接改 04-Resources/。

**事实核查：**
> 逐条检查 `30-drafts/v2/第3节.md` 中的数据、事实与案例，标注每条能否溯源到 `10-source/` 的具体文件，输出核查清单（可溯源/不可溯源/存疑三档）。禁止编造来源。

**知识库检索（只读场景）：**
> 用 vault-read 检索全库，汇总我关于"复利"的所有笔记并给出处链接。

---

## 6. 看板视图

文件列表进入 `00-System/bases/`，点击打开（`.base` 不是笔记，别用双链引用）：

| 视图 | 过滤 | 用途 |
|---|---|---|
| 共创看板 | `type == "draft"` 且在 `03-Projects/`，按 `status` 分组 | 总览所有稿子走到哪一步；装看板插件后可拖卡片改状态 |
| 待我接管 | `status == "revising"`，带 `stale` 公式列 | 提醒"轮到你了"以及停滞天数 |
| AI 参与度审计 | 有 `authored_by` 的文件 | 事后筛出 AI 高参与内容做重点核查 |

> 需 Obsidian 1.9+。若打开报错（语法随版本演进），按提示删掉报错的过滤行即可。

---

## 7. 每周维护清单

- [ ] `bash 00-System/scripts/status.sh` 看总览（或 `/query/status` 让 AI 附行动建议），处理停滞的稿子
- [ ] `/intake/triage` 清空 `01-Inbox/`（三去向，见 §4.3）
- [ ] `/produce/brief` 更新当前项目进展快照
- [ ] 有人工大改则 `/quality/voice`，审定后合并进 voice-reference
- [ ] `git add -A && git commit -m "周记：<一句话>"`

---

## 8. 常见问题与避坑

**Q：AI 说它写不了某文件？**
按设计如此。查三处：`.claude/settings.json` 的 deny、`.obsidian-agentprotected`、以及是否在 `project-rw` 边界外。

**Q：一篇文章既是领域资料，又要用于当前项目，放哪？**
放 `04-Resources/clippings/`，项目里用双链引用；只有当项目需要**改写引用**时，才复制一份进 `10-source/`。避免同一文件两处漂移。

**Q：把库移到别的盘/机器？**
同平台移动：只改 `.mcp.json` 里两个实例的 vault 绝对路径（`.claude/settings.json` 全是相对路径，不用动）。
换到 **Linux/macOS**：`cmd /c` 包装是 Windows 专有的——用根目录 `.mcp.linux.json` 覆盖 `.mcp.json`（复制改名），把 `/ABS/PATH/TO/...` 换成该机器上库的真实路径。前提：库文件已同步/挂载到那台机器（git clone 或网盘同步均可），MCP server 必须能本地读到文件。

**Q：只用外部 AI 工具、不开 Obsidian 行不行？**
文件层面完全可以（AI 经 MCP 读写不依赖 Obsidian）。但看板、双链、模板以及你在 `40-review/` 的人工修改体验都在 Obsidian 里——建议创作时保持 Obsidian 常开，它会自动感知 AI 的外部改动并刷新，双窗口无冲突（§5.2）。

**Q：AI 拦截是否绝对可靠？**
不是。分层看：外部工具路径的硬边界**只有 MCP 的 vault-root 钉死**；settings deny 拦不住 Python/Node 脚本自己 open 文件；Vault Operator 只覆盖经 Obsidian 的写入（实测其影子快照不记录外部直写）。所以敏感素材（访谈、未公开数据）本来就不该进 AI 上下文；外部写入的后悔药是 git——坚持一轮共创一 commit。

**坑 1：把创作项目放进 `02-Areas/`** → AI 写入边界失效，权限配置全乱。项目永远在 `03-Projects/`。
**坑 2：让 AI 直接润色 `10-source/` 素材** → 原始性丢失不可逆。润色版进 `20-notes/`。
**坑 3：开 Obsidian Git 自动 commit** → 一次共创被切碎成几十个 commit，还可能与未保存内容冲突。
**坑 4：只写 `_brief.md` 不写 `challenges.md`** → AI 顺着你说，产出四平八稳没有张力。
**坑 5：反复调 prompt 求好初稿，却不维护 `_brief.md`** → 反了。上下文文档才是资产，文字是可替换的。
**坑 6：把领域判断写在收藏的原文上** → 判断进 Areas（`/knowledge/note`），原文保持纯净，两者用双链互指。
**坑 7：移动端用 Obsidian Git** → 官方标注 highly unstable。移动同步用网盘类方案。
**坑 8：自己加权限规则时写 `Write(path)`** → 官方文档明确这种规则"会被接受但从不咨询"，等于没写；必须写 `Edit(path)`。

---

## 9. 如何自定义

### 9.1 新增一个领域

1. 建 `02-Areas/<领域名>/_context.md`（复制现有领域的，改 `area` 和关键词）。
2. 在 `CLAUDE.md` 的路由表加一行。完事。

### 9.2 调整 AI 权限

改 `.claude/settings.json`（规则用 `Edit(路径)`，deny → ask → allow 顺序求值）；涉及"看得见看不见"改 `.obsidian-agentignore`，"可读不可写"改 `.obsidian-agentprotected`。三处改完 commit 入库。

### 9.3 新增斜杠命令与脚本

- **命令**：复制 `.claude/commands/<功能组>/` 里任意一个 `.md` 改内容：frontmatter 的 `description` 决定 AI 何时联想调用，`argument-hint` 提示参数格式。新命令在 Claude Code / Claudian 重启会话后出现在 `/` 菜单，按功能组前缀归位。
- **脚本**：判断标准——**无需理解语义即可验证正确性的步骤**（建目录、移动、统计、改状态字段）优先写成 `00-System/scripts/` 下的 bash 脚本，命令提示词调用脚本而非让 AI 手工执行。`.gitattributes` 已强制 `*.sh` 用 LF，新脚本沿用 `#!/usr/bin/env bash` 开头即可跨 Windows/Linux 运行。

### 9.4 示例项目处理

`03-Projects/示例-NoteAI课程/` 是完整示例：留着参考，或复制改名当第一个真项目，或直接删除。

---

> 配套文档：AI 协作规范见根目录 `CLAUDE.md`；设计出处与公开参考链接见仓库 README。
