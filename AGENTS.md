# AGENTS.md

AI collaboration rules for this vault are consolidated in [CLAUDE.md](CLAUDE.md). Read it in full first and follow:

1. The directory map & permission boundaries (`02-Areas/`, `10-source/`, `40-review/`, `50-final/` are read-only / write-forbidden)
2. The hard rules (read `_brief.md` first, double-write drafts to `_baseline/`, AI participation labels, the state machine)
3. The area routing table (locate `_context.md` by keywords — never blind-scan the vault)

Governance files (this file, CLAUDE.md, `.claude/settings.json`, `.mcp.json`, `.obsidian-agent*`) are version-controlled — **never modify them**.
