# AGENTS.md

本 vault 的 AI 协作规范统一在 [CLAUDE.md](CLAUDE.md)，请先完整阅读并遵守其中的：

1. 目录地图与权限边界（`02-Areas/`、`10-source/`、`40-review/`、`50-final/` 均为只读/禁写）
2. 硬性规则（先读 `_brief.md`、初稿双写 `_baseline/`、AI 参与度标注、状态机）
3. 领域路由表（按关键词定位 `_context.md`，不要盲扫全库）

治理文件（本文件、CLAUDE.md、`.claude/settings.json`、`.mcp.json`、`.obsidian-agent*`）本身受版本控制，**禁止修改**。
